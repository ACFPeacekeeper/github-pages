/**
 * Placeholder router factory for multi-framework experiments.
 * Docusaurus owns primary routing; this documents the parity slot with
 * the main site's createAppRouter / Vue createAppRouter helpers.
 */
export function createAppRouterNote(): string {
  return 'Primary routing is Docusaurus; use this module for experimental SPA islands only.';
}
