# stack/eslint/

ESLint flat config for the documentation website package. Root
`docs/website/eslint.config.js` re-exports `eslint.config.js` here
(same discovery pattern as the main site's `stack/eslint/`).
