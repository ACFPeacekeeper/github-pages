# Local stack: builds the static export and serves it via nginx.
#
# Usage (from the repo root):
#   docker compose -f infra/docker/docker-compose.yml up --build

services:
  site:
    build:
      context: ..
      dockerfile: infra/docker/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
