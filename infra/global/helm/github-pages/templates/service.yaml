apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-pages.name" . }}
  labels:
    {{- include "github-pages.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "github-pages.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
