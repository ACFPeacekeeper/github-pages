# stack/

Framework and tooling packages for this site’s primary stack.

| Directory | Role |
| --- | --- |
| [`eslint/`](eslint/) | ESLint config (Next core-web-vitals) |
| [`next/`](next/) | Next.js config + `next-env.d.ts` |

Thin re-exports at the repository root keep `next`, `next lint`, and ESLint discovery working.
